<Page
    x:Class="OpenScribe.App.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:OpenScribe.Core.Models"
    NavigationCacheMode="Enabled"
    Loaded="Page_Loaded">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Dashboard" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock Text="Recent capture sessions" Style="{StaticResource SubtitleTextBlockStyle}" Opacity="0.7" />
        </StackPanel>

        <!-- Quick Actions -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12" Margin="0,0,0,16">
            <Button Content="New Capture" Style="{StaticResource AccentButtonStyle}" Click="NewCapture_Click">
                <Button.KeyboardAccelerators>
                    <KeyboardAccelerator Modifiers="Control" Key="N" />
                </Button.KeyboardAccelerators>
            </Button>
            <Button Content="Refresh" Click="Refresh_Click" />
        </StackPanel>

        <!-- Sessions List -->
        <ListView Grid.Row="2"
                  x:Name="SessionsList"
                  SelectionMode="None"
                  ItemsSource="{x:Bind ViewModel.RecentSessions, Mode=OneWay}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:CaptureSession">
                    <Grid Padding="12" Margin="0,4" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" FontSize="16" />
                            <StackPanel Orientation="Horizontal" Spacing="16" Margin="0,4,0,0">
                                <TextBlock Text="{x:Bind CreatedAtDisplay}" Opacity="0.6" />
                                <TextBlock Text="{x:Bind StatusDisplay}" Opacity="0.6" />
                                <TextBlock Opacity="0.6">
                                    <Run Text="{x:Bind StepCount}" />
                                    <Run Text=" steps" />
                                </TextBlock>
                            </StackPanel>
                        </StackPanel>
                        <Button Grid.Column="1" Content="Review" Margin="8,0" Tag="{x:Bind Id}" Click="ReviewSession_Click" />
                        <Button Grid.Column="2" Content="Delete" Tag="{x:Bind Id}" Click="DeleteSession_Click" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Status -->
        <TextBlock Grid.Row="3" Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" Opacity="0.6" Margin="0,8,0,0" />
    </Grid>
</Page>
